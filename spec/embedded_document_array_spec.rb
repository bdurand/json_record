require File.expand_path(File.join(File.dirname(__FILE__), 'spec_helper'))

describe JsonRecord::EmbeddedDocumentArray do
  
  it "should be an Array"
  
  it "should convert hashes to objects on initialize and set the parent"
  
  it "should convert hashes to objects on concat and set the parent"
  
  it "should convert hashes to objects on append and set the parent"
  
  it "should not accept objects that are not the correct class"
  
  it "should have a build method that appends the values and set the parent"
  
end
