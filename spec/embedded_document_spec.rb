require File.expand_path(File.join(File.dirname(__FILE__), 'spec_helper'))

describe JsonRecord::EmbeddedDocument do
  
  it "should have a schema"
  
  it "should have a parent object"
  
  it "should accept validations"
  
  it "should have attributes"
  
  it "should have attributes before type cast"
  
  it "should track changes to attributes"
  
  it "should convert to attributes to json"
  
  it "should consider it equal to another EmbeddedDocument with the same attributes and parent"
  
end
