require File.expand_path(File.join(File.dirname(__FILE__), 'spec_helper'))

describe JsonRecord::Schema do
  
  it "should define single key accessor methods"
  
  it "should define many accessor methods"
  
  it "should validate the presence of an attribute"
  
  it "should validate the length of an attribute"
  
  it "should validate that an attribute is in a value"
  
  it "should validate the type of an attribute"
  
  it "should validate that an attribute is in a range"
  
  it "should validate the format of an attribute"
  
end
